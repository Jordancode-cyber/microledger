
module.exports = ({ config }) => {
  return {
    ...config,
    // You can add other Expo configurations here later if needed,
    // but environment variables are now handled automatically!
  };
};